APP_NAME=Laravel
APP_ENV=testing
APP_KEY=base64:O/+nUzkmcD2/5kPI2Uh0RaHDXsSVTg3zcD0o2zsxU4U=
# PRODUCTION SECURITY: Set to false in production
APP_DEBUG=false
APP_URL=https://coprra.com
APP_TIMEZONE=UTC
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Security Headers and HTTPS Enforcement
APP_FORCE_HTTPS=true
APP_HSTS_MAX_AGE=31536000
APP_CONTENT_SECURITY_POLICY=true

# CDN configuration
# Set ASSET_URL to your CDN base to serve assets via CDN
ASSET_URL=
# Single primary CDN URL; used as fallback if ASSET_URL not set
CDN_URL=
# Optional: multiple CDN base URLs (comma-separated) for failover/rotation
CDN_URLS=
# Toggle CDN asset delivery helper logic (reserved for future use)
CDN_ENABLED=false
# Failover attempt flag if primary is unreachable
CDN_FAILOVER=true
# Timeout for CDN health checks in milliseconds
CDN_TIMEOUT_MS=500

# CORS configuration
# Comma-separated list of allowed origins. Example:
#   http://localhost:5173,http://127.0.0.1:5173,https://frontend.example.com
CORS_ALLOWED_ORIGINS=

# Comma-separated HTTP methods allowed for CORS requests.
# Defaults to REST methods and OPTIONS for preflight
CORS_ALLOWED_METHODS=GET,POST,PUT,PATCH,DELETE,OPTIONS

# Comma-separated list of request headers allowed
CORS_ALLOWED_HEADERS=Accept,Authorization,Content-Type,X-Requested-With,X-CSRF-TOKEN

# Comma-separated list of response headers exposed to the browser
CORS_EXPOSED_HEADERS=

# Seconds to cache preflight responses
CORS_MAX_AGE=600

# Whether to send cookies/authorization headers on cross-origin requests
# Enable only if required (e.g., Sanctum SPA auth)
CORS_SUPPORTS_CREDENTIALS=true

# Frontend URL for production if different from APP_URL
FRONTEND_URL=

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=single
LOG_LEVEL=info
LOG_STDERR_FORMATTER=
LOG_EMERGENCY_CHANNEL=single
LOG_AUDIT_ENABLED=true
LOG_SECURITY_ENABLED=true
LOG_PERFORMANCE_ENABLED=true
LOG_MAX_FILES=30
LOG_DAILY_ROTATION=true

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=coprra_test
DB_USERNAME=root
DB_PASSWORD=rootpassword
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci
DB_PREFIX=
DB_STRICT_MODE=true
DB_ENGINE=InnoDB
DB_TIMEZONE=+00:00
DB_SSL_MODE=REQUIRED
DB_SSL_CERT=
DB_SSL_KEY=
DB_SSL_CA=
DB_CONNECTION_TIMEOUT=60
DB_READ_TIMEOUT=60
DB_WRITE_TIMEOUT=60

BROADCAST_DRIVER=redis
CACHE_DRIVER=array
CACHE_PREFIX=coprra_cache
CACHE_TTL=3600
FILESYSTEM_DISK=s3
QUEUE_CONNECTION=sync
QUEUE_FAILED_DRIVER=database
SESSION_DRIVER=array
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=strict

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=redis
REDIS_PASSWORD=
REDIS_PORT=6379
REDIS_CLIENT=phpredis
REDIS_CLUSTER=false
REDIS_PREFIX=coprra:
REDIS_DB=0
REDIS_CACHE_DB=1
REDIS_SESSION_DB=2
REDIS_QUEUE_DB=3
REDIS_TIMEOUT=5
REDIS_READ_TIMEOUT=60
REDIS_PERSISTENT=true
REDIS_SSL_ENABLED=false
REDIS_SSL_CERT=
REDIS_SSL_KEY=
REDIS_SSL_CA=

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@coprra.com"
MAIL_FROM_NAME="${APP_NAME}"
MAIL_REPLY_TO_ADDRESS="support@coprra.com"
MAIL_REPLY_TO_NAME="${APP_NAME} Support"
MAIL_TIMEOUT=30
MAIL_LOCAL_DOMAIN=coprra.com
MAIL_VERIFY_PEER=true
MAIL_ALLOW_SELF_SIGNED=false

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=coprra-production
AWS_USE_PATH_STYLE_ENDPOINT=false
AWS_URL=
AWS_ENDPOINT=
AWS_S3_REGION=${AWS_DEFAULT_REGION}
AWS_S3_BUCKET=${AWS_BUCKET}
AWS_S3_URL=
AWS_S3_ENDPOINT=
AWS_CLOUDFRONT_DISTRIBUTION_ID=
AWS_CLOUDFRONT_DOMAIN=
AWS_SES_REGION=${AWS_DEFAULT_REGION}
AWS_SES_CONFIGURATION_SET=
AWS_SNS_REGION=${AWS_DEFAULT_REGION}
AWS_SQS_REGION=${AWS_DEFAULT_REGION}
AWS_SQS_PREFIX=https://sqs.${AWS_DEFAULT_REGION}.amazonaws.com/${AWS_ACCOUNT_ID}

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

REQUIRE_2FA=true

# ============================================================================
# Security and Monitoring Configuration
# ============================================================================

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX_ATTEMPTS=60
RATE_LIMIT_DECAY_MINUTES=1
RATE_LIMIT_PREFIX=throttle

# CSRF Protection
CSRF_ENABLED=true
CSRF_TOKEN_LIFETIME=7200
CSRF_REGENERATE_TOKEN=true

# XSS Protection
XSS_PROTECTION_ENABLED=true
CONTENT_TYPE_NOSNIFF=true
FRAME_OPTIONS=DENY
REFERRER_POLICY=strict-origin-when-cross-origin

# SQL Injection Protection
SQL_INJECTION_PROTECTION=true
PREPARED_STATEMENTS_ONLY=true

# File Upload Security
MAX_UPLOAD_SIZE=10240
ALLOWED_FILE_TYPES=jpg,jpeg,png,webp,pdf,doc,docx
SCAN_UPLOADS_FOR_MALWARE=true
QUARANTINE_SUSPICIOUS_FILES=true

# API Security
API_AUTHENTICATION_REQUIRED=true
API_RATE_LIMITING=true
API_REQUEST_SIGNING=true
API_RESPONSE_ENCRYPTION=true

# Monitoring and Alerting
MONITORING_ENABLED=true
PERFORMANCE_MONITORING=true
ERROR_TRACKING=true
UPTIME_MONITORING=true
SECURITY_MONITORING=true

# Google Analytics
GOOGLE_ANALYTICS_ID=

# Backup and Recovery
AUTOMATED_BACKUPS=true
BACKUP_FREQUENCY=daily
BACKUP_RETENTION_DAYS=30
BACKUP_ENCRYPTION=true
DISASTER_RECOVERY_ENABLED=true

# ============================================================================
# COPRRA Configuration
# ============================================================================
# Advanced Price Comparison Platform Settings

# COPRRA Application Settings
COPRRA_NAME=COPRRA
COPRRA_DEFAULT_CURRENCY=USD
COPRRA_DEFAULT_LANGUAGE=en
COPRRA_DEFAULT_CONDITION=new
COPRRA_MAX_PRICE_RANGE=10000
COPRRA_MIN_PRICE_RANGE=1

# Price Comparison Settings
PRICE_CACHE_DURATION=3600
MAX_STORES_PER_PRODUCT=10
PRICE_UPDATE_INTERVAL=6

# Search and Filtering Settings
SEARCH_MAX_RESULTS=50
SEARCH_MIN_QUERY_LENGTH=2
SEARCH_ENABLE_AUTOCOMPLETE=true

# API Configuration
API_RATE_LIMIT=100
API_VERSION=v1
API_ENABLE_DOCS=true

# Exchange Rates (Base: USD = 1.0)
EXCHANGE_RATE_USD=1.0
EXCHANGE_RATE_EUR=0.85
EXCHANGE_RATE_GBP=0.73
EXCHANGE_RATE_JPY=110.0
EXCHANGE_RATE_SAR=3.75
EXCHANGE_RATE_AED=3.67
EXCHANGE_RATE_EGP=30.9

# Pagination and Limits
DEFAULT_ITEMS_PER_PAGE=20
MAX_WISHLIST_ITEMS=100
MAX_PRICE_ALERTS=50

# Image and Media Settings
MAX_IMAGE_SIZE=2048
# Allowed image types: jpg, jpeg, png, webp (defined in config)

# Analytics and Tracking
GOOGLE_ANALYTICS_ID=
TRACK_USER_BEHAVIOR=true
TRACK_PRICE_CLICKS=true

# Security Settings
# ENABLE_2FA: Allow users to enable 2FA
# REQUIRE_2FA: Force all users to use 2FA
ENABLE_2FA=true
PASSWORD_MIN_LENGTH=12
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_NUMBERS=true
PASSWORD_REQUIRE_SYMBOLS=true
PASSWORD_HISTORY_COUNT=5
PASSWORD_EXPIRY_DAYS=90
SESSION_TIMEOUT=30
ACCOUNT_LOCKOUT_ATTEMPTS=5
ACCOUNT_LOCKOUT_DURATION=30
FORCE_PASSWORD_CHANGE_ON_FIRST_LOGIN=true
SUSPICIOUS_ACTIVITY_MONITORING=true

# Performance Settings
ENABLE_QUERY_CACHING=true
ENABLE_VIEW_CACHING=true
ENABLE_COMPRESSION=true

# Contact Information
CONTACT_PHONE="+966 50 123 4567"
CONTACT_EMAIL="info@coprra.com"
CONTACT_ADDRESS="الرياض، المملكة العربية السعودية"
CONTACT_WORKING_HOURS="9:00 ص - 6:00 م"

# External Services (Optional)
EXCHANGE_RATE_API_KEY=
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest/USD

# Health checks
HEALTH_CHECKS_ENDPOINT=/api/health

# Session and Cookie settings for cross-origin SPA (development defaults)
SESSION_SAME_SITE=lax
SESSION_SECURE_COOKIE=false
SESSION_DOMAIN=
SESSION_PATH=/

# ============================================================================
# Store Adapters Configuration
# ============================================================================

# Amazon Product Advertising API
AMAZON_API_KEY=
AMAZON_API_SECRET=
AMAZON_ASSOCIATE_TAG=
AMAZON_REGION=us-east-1

# eBay API
EBAY_APP_ID=
EBAY_CERT_ID=
EBAY_DEV_ID=
EBAY_SITE_ID=0

# Noon API (Middle East)
NOON_API_KEY=
NOON_COUNTRY=ae

# ============================================================================
# Error Tracking and Performance Monitoring
# ============================================================================
SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
SENTRY_PROFILES_SAMPLE_RATE=0.1
SENTRY_ENVIRONMENT=${APP_ENV}
SENTRY_RELEASE=
SENTRY_SERVER_NAME=
SENTRY_SEND_DEFAULT_PII=false
SENTRY_ATTACH_STACKTRACE=true
SENTRY_CAPTURE_UNHANDLED_REJECTIONS=true

# ============================================================================
# Environment-Specific Configuration
# ============================================================================

# Development Environment
DEV_TOOLBAR_ENABLED=false
DEV_DEBUG_BAR_ENABLED=false
DEV_TELESCOPE_ENABLED=false
DEV_CLOCKWORK_ENABLED=false

# Staging Environment
STAGING_BASIC_AUTH_ENABLED=false
STAGING_BASIC_AUTH_USER=
STAGING_BASIC_AUTH_PASSWORD=

# Production Environment
PRODUCTION_MAINTENANCE_MODE=false
PRODUCTION_CACHE_CONFIG=true
PRODUCTION_CACHE_ROUTES=true
PRODUCTION_CACHE_VIEWS=true
PRODUCTION_OPTIMIZE_AUTOLOADER=true
PRODUCTION_ASSET_VERSIONING=true

# ============================================================================
# Deployment and Infrastructure
# ============================================================================

# Load Balancer Configuration
LOAD_BALANCER_ENABLED=false
LOAD_BALANCER_HEALTH_CHECK_PATH=/health
LOAD_BALANCER_STICKY_SESSIONS=true

# Auto Scaling Configuration
AUTO_SCALING_ENABLED=false
AUTO_SCALING_MIN_INSTANCES=2
AUTO_SCALING_MAX_INSTANCES=10
AUTO_SCALING_TARGET_CPU=70

# Container Configuration
CONTAINER_MEMORY_LIMIT=512M
CONTAINER_CPU_LIMIT=1
CONTAINER_RESTART_POLICY=always
CONTAINER_HEALTH_CHECK_ENABLED=true

# Kubernetes Configuration
K8S_NAMESPACE=coprra-production
K8S_SERVICE_ACCOUNT=coprra-service
K8S_INGRESS_CLASS=nginx
K8S_TLS_SECRET_NAME=coprra-tls

# ============================================================================
# Feature Flags and A/B Testing
# ============================================================================

# Feature Flags
FEATURE_NEW_UI=false
FEATURE_ADVANCED_SEARCH=true
FEATURE_PRICE_PREDICTIONS=false
FEATURE_SOCIAL_LOGIN=true
FEATURE_MOBILE_APP_API=true

# A/B Testing
AB_TESTING_ENABLED=false
AB_TESTING_PROVIDER=
AB_TESTING_PROJECT_ID=

# ============================================================================
# Compliance and Legal
# ============================================================================

# GDPR Compliance
GDPR_ENABLED=true
GDPR_COOKIE_CONSENT=true
GDPR_DATA_RETENTION_DAYS=365
GDPR_RIGHT_TO_BE_FORGOTTEN=true

# Cookie Policy
COOKIE_POLICY_ENABLED=true
COOKIE_POLICY_STRICT_MODE=true
COOKIE_POLICY_ANALYTICS_CONSENT=required

# Terms and Privacy
TERMS_VERSION=1.0
PRIVACY_POLICY_VERSION=1.0
FORCE_TERMS_ACCEPTANCE=true


